package  ${defaultTemplate.packagePath};

import ${defaultTemplate.modelPath};

/**
 * @description 
 * @author  wang yang
 */
@SuppressWarnings("serial")
public class ${defaultTemplate.voName} extends ${defaultTemplate.entityName}{

	//~ fields =======================================================
	
}