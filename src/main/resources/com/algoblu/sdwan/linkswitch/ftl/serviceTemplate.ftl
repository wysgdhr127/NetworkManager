package  ${defaultTemplate.packagePath};

import org.springframework.stereotype.Service;
import ${defaultTemplate.baseServicePath};
import ${defaultTemplate.voPath};

/**
 * @description 
 * @author  wang yang
 */
@Service
public interface ${defaultTemplate.serviceName} extends ${defaultTemplate.baseServiceName}<${defaultTemplate.voName}>{

	//~ User-defined Method =======================================================
		
}